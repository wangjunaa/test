apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: rpc-test
  region: ap-southeast-1

fargateProfiles:
  - name: fp-default
    selectors:
      - namespace: default
      - namespace: grpcserver  # 添加这个行